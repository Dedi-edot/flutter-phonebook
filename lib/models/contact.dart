class Contact {
  int? id;
  final name;
  final phone;
  final email;
  final company;
  final job;
  final image;
  bool isFav;

  Contact(this.id, this.name, this.phone, this.email, this.company, this.job, this.image,
      this.isFav);
}
